{
  "apiVersion": "samakia.fabric/v1alpha1",
  "kind": "TenantConsumerBinding",
  "metadata": {
    "tenant_id": "<tenant-id>",
    "consumer": "vector"
  },
  "spec": {
    "consumer": "vector",
    "variant": "single",
    "ha_ready": true,
    "mode": "dry-run",
    "endpoint_ref": "vector-primary",
    "secret_ref": "secrets/tenants/<tenant-id>/vector-primary",
    "backup_target": "backup://<tenant-id>/vector",
    "restore_testcases": ["restore-from-backup"],
    "dr_testcases": ["gameday:vip-failover"],
    "owner": {
      "tenant_id": "<tenant-id>",
      "consumer": "vector"
    }
  }
}
