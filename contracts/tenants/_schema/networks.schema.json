{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TenantNetworks",
  "type": "object",
  "additionalProperties": false,
  "required": ["apiVersion", "kind", "metadata", "spec"],
  "properties": {
    "apiVersion": {"type": "string"},
    "kind": {"const": "TenantNetworks"},
    "metadata": {
      "type": "object",
      "additionalProperties": false,
      "required": ["tenant_id"],
      "properties": {
        "tenant_id": {"type": "string"}
      }
    },
    "spec": {
      "type": "object",
      "additionalProperties": false,
      "required": ["segments", "cross_tenant_access"],
      "properties": {
        "segments": {"type": "array", "items": {"type": "string"}},
        "cross_tenant_access": {"type": "boolean"}
      }
    }
  }
}
