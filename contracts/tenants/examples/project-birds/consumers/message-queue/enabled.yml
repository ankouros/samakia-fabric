{
  "consumer": "message-queue",
  "variant": "single",
  "ha_ready": true,
  "executor": {
    "provider": "rabbitmq",
    "mode": "dry-run",
    "plan_only": true
  },
  "dr": {
    "required_testcases": ["backup_integrity", "restore_smoke"],
    "backup": {
      "schedule": "<placeholder>",
      "retention": "<placeholder>"
    },
    "restore_verification": {
      "smoke": "<placeholder>"
    }
  },
  "endpoints": {
    "host": "mq.project-birds.internal",
    "port": 5672,
    "protocol": "tcp",
    "tls_required": true
  },
  "secret_ref": "tenants/project-birds/message-queue/primary",
  "resources": {}
}
