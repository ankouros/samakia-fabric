{
  "consumer": "database",
  "variant": "single",
  "ha_ready": true,
  "executor": {
    "provider": "postgres",
    "mode": "dry-run",
    "plan_only": true
  },
  "dr": {
    "required_testcases": ["backup_integrity", "restore_smoke"],
    "backup": {
      "schedule": "<placeholder>",
      "retention": "<placeholder>"
    },
    "restore_verification": {
      "smoke": "<placeholder>"
    }
  },
  "endpoints": {
    "host": "db.samakia-internal-tools.internal",
    "port": 5432,
    "protocol": "tcp",
    "tls_required": true
  },
  "secret_ref": "tenants/samakia-internal-tools/database/primary",
  "resources": {}
}
