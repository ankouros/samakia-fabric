{
  "allowed_envs": ["samakia-dev", "samakia-staging", "samakia-shared"],
  "allowed_tenants": ["samakia-internal-tools", "project-birds"],
  "allowed_consumers": ["database", "message-queue", "cache", "vector"],
  "allowed_providers": {
    "database": ["postgres", "mariadb"],
    "message-queue": ["rabbitmq"],
    "cache": ["dragonfly"],
    "vector": ["qdrant"]
  },
  "allowed_variants": ["single", "cluster"],
  "allow_dr_execute": false,
  "require_reason": true,
  "require_change_window_for_prod": true,
  "require_signing_for_prod": true,
  "require_signing_for_shared": false
}
